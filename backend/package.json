{
  "name": "shopping-report-builder",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc -p tsconfig.backend.json && npm run copy-files",
    "copy-files": "cpx appsscript.json ../dist"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "typescript": "~5.5.2"
  }
}
